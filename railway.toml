buildCommand = "npm install -g pnpm@10 && ln -sf $(npm config get prefix)/bin/pnpm /usr/local/bin/pnpm && pnpm install --frozen-lockfile && BASE_PATH=/ NODE_ENV=production pnpm --filter @workspace/boutique run build && pnpm --filter @workspace/api-server run build"
